# Dockerfile simplificado - Asume que el JAR ya está construido localmente
FROM eclipse-temurin:17-jre-jammy

WORKDIR /app

# Copy the pre-built jar from local build
COPY build/libs/*.jar app.jar

# Expose port
EXPOSE 8080

# Run the application
CMD ["java", "-jar", "app.jar"]

